@model Proyecto_MVC_Falabella.Models.Compania

@{
    ViewBag.Title = "Detalles de la compañia";
}

<h2>@ViewBag.Title</h2>

<div>
    <h4>Compañia: @Model.Nombre</h4>
    <hr />

          <img src="@Model.Imagen" style="width:300px; height:300px; object-fit: cover;" alt="Sin Imagen" />

    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Consecutivo)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Consecutivo)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Nombre)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Nombre)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Descripcion)
        </dt>

        <dd>
            @*@Html.DisplayFor(model => model.Descripcion)*@
@if(string.IsNullOrWhiteSpace(Model.Descripcion))
{
<text>N/A</text>
}
else
{
            @Html.DisplayFor(modelItem => Model.Descripcion)
}
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Editar", "Editar", new { id = Model.id }) |
    @Html.ActionLink("Atras", "Listar")
</p>
